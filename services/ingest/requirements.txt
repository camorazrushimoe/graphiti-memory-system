fastapi==0.115.0
uvicorn[standard]==0.32.0
asyncpg==0.29.0
pydantic>=2.9,<3
python-dateutil==2.9.0.post0

# Layer 7 — Retrieval endpoint (/retrieve): semantic search (Qdrant) +
# graph traversal (Graphiti/Neo4j). Same pinned versions as the compiler
# service (services/compiler/requirements.txt) since both now import the
# shared client config in common/graphiti_config.py + common/qdrant_client.py.
openai==2.47.0
graphiti-core==0.29.2
qdrant-client==1.18.0
